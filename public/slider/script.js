$(".slider").slick({

  dots: false,
  infinite: true,
  slidesToShow: 1,
  dots: false,
  // customPaging : function(slider, i) {
  //     var thumb = $(slider.$slides[i]).data('thumb');
  //     return '<a><img src="'+thumb+'"></a>';
  // },

  
});